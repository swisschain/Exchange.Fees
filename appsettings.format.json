{
  "FeesService": {
    "Db": {
      "ConnectionString": "${ExchangeFeesServiceDataConnectionString}"
    },
    "RabbitMq": {
      "HostUrl": "${ExchangeRabbitMqHostUrl}",
      "Username": "${ExchangeRabbitUser}",
      "Password": "${ExchangeRabbitPassword}"
    },
    "AccountsService": {
      "GrpcUrl": "${Exchange.Accounts.Grpc.ConnectionUrl}"
    },
  },
  "Jwt": {
    "Secret": "${JwtTokenSecret}"
  }
}